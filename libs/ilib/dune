(include_subdirs no)

(env
	(_
		(flags (-w -3 -w -27))
	)
)

(library
	(name ilib)
	(modules_without_implementation ilData ilMeta)
	(modules (:standard \ dump))
	(libraries extlib)
	(wrapped false)
)
