package cs.system.security.claims;

/** Defines claim value types according to the type URIs defined by W3C and OASIS. This class cannot be inherited. */
@:native("System.Security.Claims.ClaimValueTypes")
extern class ClaimValueTypes {
	/** A URI that represents the  XML data type. */
	static var Base64Binary(default, never):String;
	/** A URI that represents the  XML data type. */
	static var Base64Octet(default, never):String;
	/** A URI that represents the  XML data type. */
	static var Boolean(default, never):String;
	/** A URI that represents the  XML data type. */
	static var Date(default, never):String;
	/** A URI that represents the  XML data type. */
	static var DateTime(default, never):String;
	/** A URI that represents the  XQuery data type. */
	static var DaytimeDuration(default, never):String;
	/** A URI that represents the  SOAP data type. */
	static var DnsName(default, never):String;
	/** A URI that represents the  XML data type. */
	static var Double(default, never):String;
	/** A URI that represents the  XML Signature data type. */
	static var DsaKeyValue(default, never):String;
	/** A URI that represents the  SOAP data type. */
	static var Email(default, never):String;
	/** A URI that represents the  XML data type. */
	static var Fqbn(default, never):String;
	/** A URI that represents the  XML data type. */
	static var HexBinary(default, never):String;
	/** A URI that represents the  XML data type. */
	static var Integer(default, never):String;
	/** A URI that represents the  XML data type. */
	static var Integer32(default, never):String;
	/** A URI that represents the  XML data type. */
	static var Integer64(default, never):String;
	/** A URI that represents the  XML Signature data type. */
	static var KeyInfo(default, never):String;
	/** A URI that represents the  XACML 1.0 data type. */
	static var Rfc822Name(default, never):String;
	/** A URI that represents the  SOAP data type. */
	static var Rsa(default, never):String;
	/** A URI that represents the  XML Signature data type. */
	static var RsaKeyValue(default, never):String;
	/** A URI that represents the  XML data type. */
	static var Sid(default, never):String;
	/** A URI that represents the  XML data type. */
	static var String(default, never):String;
	/** A URI that represents the  XML data type. */
	static var Time(default, never):String;
	/** A URI that represents the  XML data type. */
	static var UInteger32(default, never):String;
	/** A URI that represents the  XML data type. */
	static var UInteger64(default, never):String;
	/** A URI that represents the  SOAP data type. */
	static var UpnName(default, never):String;
	/** A URI that represents the  XACML 1.0 data type. */
	static var X500Name(default, never):String;
	/** A URI that represents the  XQuery data type. */
	static var YearMonthDuration(default, never):String;
}
