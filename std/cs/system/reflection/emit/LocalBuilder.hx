package cs.system.reflection.emit;

/** Represents a local variable within a method or constructor. */
@:native("System.Reflection.Emit.LocalBuilder")
extern class LocalBuilder extends cs.system.reflection.LocalVariableInfo {
}
