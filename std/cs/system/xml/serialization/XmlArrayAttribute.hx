package cs.system.xml.serialization;

/** Specifies that the  must serialize a particular class member as an array of XML elements. */
@:native("System.Xml.Serialization.XmlArrayAttribute")
extern class XmlArrayAttribute extends cs.system.Attribute {
	/**
	 * Gets or sets the XML element name given to the serialized array.
	 * @return The XML element name of the serialized array. The default is the name of
	 * the member to which the  is assigned.
	 */
	var ElementName(default, default):String;
	/**
	 * Gets or sets a value that indicates whether the XML element name generated by
	 * the  is qualified or unqualified.
	 * @return One of the  values. The default is .
	 */
	var Form(default, default):cs.system.xml.schema.XmlSchemaForm;
	/**
	 * Gets or sets a value that indicates whether the  must serialize a member as an
	 * empty XML tag with the  attribute set to .
	 * @return if the  generates the  attribute; otherwise, .
	 */
	var IsNullable(default, default):Bool;
	/**
	 * Gets or sets the namespace of the XML element.
	 * @return The namespace of the XML element.
	 */
	var Namespace(default, default):String;
	/**
	 * Gets or sets the explicit order in which the elements are serialized or
	 * deserialized.
	 * @return The order of the code generation.
	 */
	var Order(default, default):Int;
	@:overload(function():Void {})
	function new(elementName:String):Void;
}
