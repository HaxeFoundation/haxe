package cs.system.xml.serialization;

/** Indicates to the  that the member must be treated as XML text when the class that contains it is serialized or deserialized. */
@:native("System.Xml.Serialization.XmlTextAttribute")
extern class XmlTextAttribute extends cs.system.Attribute {
	/**
	 * Gets or sets the XML Schema definition language (XSD) data type of the text
	 * generated by the .
	 * @return An XML Schema (XSD) data type.
	 */
	var DataType(default, default):String;
	/**
	 * Gets or sets the type of the member.
	 * @return The  of the member.
	 */
	var Type(default, default):cs.system.Type;
	@:overload(function():Void {})
	function new(type:cs.system.Type):Void;
}
