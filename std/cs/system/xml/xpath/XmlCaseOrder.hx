package cs.system.xml.xpath;

/** Specifies the sort order for uppercase and lowercase letters. */
@:native("System.Xml.XPath.XmlCaseOrder")
extern enum XmlCaseOrder {
	LowerFirst;
	None;
	UpperFirst;
}
