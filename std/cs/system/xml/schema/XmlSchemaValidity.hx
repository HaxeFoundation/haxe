package cs.system.xml.schema;

/** Represents the validity of an XML item validated by the  class. */
@:native("System.Xml.Schema.XmlSchemaValidity")
extern enum XmlSchemaValidity {
	Invalid;
	NotKnown;
	Valid;
}
