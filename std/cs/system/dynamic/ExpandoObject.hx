package cs.system.dynamic;

/** Represents an object whose members can be dynamically added and removed at run time. */
@:native("System.Dynamic.ExpandoObject")
extern class ExpandoObject {
	function new():Void;
}
