package cs.system.linq;

/** Represents an expression tree and provides functionality to execute the expression tree after rewriting it. */
@:native("System.Linq.EnumerableExecutor")
extern class EnumerableExecutor {
}
