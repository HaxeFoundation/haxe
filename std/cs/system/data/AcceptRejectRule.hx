package cs.system.data;

/** Determines the action that occurs when the  or  method is invoked on a  with a . */
@:native("System.Data.AcceptRejectRule")
extern enum AcceptRejectRule {
	Cascade;
	None;
}
