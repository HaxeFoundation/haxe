package cs.system.data;

/** Indicates the schema serialization mode for a typed . */
@:native("System.Data.SchemaSerializationMode")
extern enum SchemaSerializationMode {
	ExcludeSchema;
	IncludeSchema;
}
