package cs.system.data.common;

/** Provides data for the  event of a .NET Framework data provider. */
@:native("System.Data.Common.RowUpdatedEventArgs")
extern class RowUpdatedEventArgs extends cs.system.EventArgs {
	/**
	 * Gets the  executed when  is called.
	 * @return The  executed when  is called.
	 */
	var Command(default, never):cs.system.data.IDbCommand;
	/**
	 * Gets any errors generated by the .NET Framework data provider when the  was
	 * executed.
	 * @return The errors generated by the .NET Framework data provider when the  was
	 * executed.
	 */
	var Errors(default, default):cs.system.Exception;
	/**
	 * Gets the number of rows changed, inserted, or deleted by execution of the SQL
	 * statement.
	 * @return The number of rows changed, inserted, or deleted; 0 if no rows were
	 * affected or the statement failed; and -1 for SELECT statements.
	 */
	var RecordsAffected(default, never):Int;
	/**
	 * Gets the  sent through an .
	 * @return The  sent through an .
	 */
	var Row(default, never):cs.system.data.DataRow;
	/**
	 * Gets the number of rows processed in a batch of updated records.
	 * @return that specifies the number of row processed.
	 */
	var RowCount(default, never):Int;
	/**
	 * Gets the type of SQL statement executed.
	 * @return The type of SQL statement executed.
	 */
	var StatementType(default, never):cs.system.data.StatementType;
	/**
	 * Gets the  of the  property.
	 * @return One of the  values. The default is .
	 */
	var Status(default, default):cs.system.data.UpdateStatus;
	/**
	 * Gets the  sent through an .
	 * @return The  sent through an .
	 */
	var TableMapping(default, never):cs.system.data.common.DataTableMapping;
	function new(dataRow:cs.system.data.DataRow, command:cs.system.data.IDbCommand, statementType:cs.system.data.StatementType, tableMapping:cs.system.data.common.DataTableMapping):Void;
	@:overload(function(array:cs.NativeArray<cs.system.data.DataRow>):Void {})
	/**
	 * Copies references to the modified rows into the provided array.
	 * @param array The array of  to copy into.
	 */
	function CopyToRows(array:cs.NativeArray<cs.system.data.DataRow>, arrayIndex:Int):Void;
}
