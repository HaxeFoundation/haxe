package cs.system.collections.specialized;

/** Notifies listeners of dynamic changes, such as when an item is added and removed or the whole list is cleared. */
@:native("System.Collections.Specialized.INotifyCollectionChanged")
extern interface INotifyCollectionChanged {
}
