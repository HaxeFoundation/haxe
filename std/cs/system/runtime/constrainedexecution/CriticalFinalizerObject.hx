package cs.system.runtime.constrainedexecution;

/** Ensures that all finalization code in derived classes is marked as critical. */
@:native("System.Runtime.ConstrainedExecution.CriticalFinalizerObject")
extern class CriticalFinalizerObject {
}
