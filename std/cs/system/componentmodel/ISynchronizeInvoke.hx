package cs.system.componentmodel;

/** Provides a way to synchronously or asynchronously execute a delegate. */
@:native("System.ComponentModel.ISynchronizeInvoke")
extern interface ISynchronizeInvoke {
	/**
	 * Gets a value indicating whether the caller must call  when calling an object
	 * that implements this interface.
	 * @return if the caller must call ; otherwise, .
	 */
	var InvokeRequired(default, never):Bool;
	/**
	 * Asynchronously executes the delegate on the thread that created this object.
	 * @param method A  to a method that takes parameters of the same number and type
	 * that are contained in .
	 * @param args An array of type  to pass as arguments to the given method. This can
	 * be  if no arguments are needed.
	 * @return An  interface that represents the asynchronous operation started by
	 * calling this method.
	 */
	function BeginInvoke(method:cs.system.Delegate, args:cs.NativeArray<Dynamic>):cs.system.IAsyncResult;
	/**
	 * Waits until the process started by calling  completes, and then returns the
	 * value generated by the process.
	 * @param result An  interface that represents the asynchronous operation started
	 * by calling .
	 * @return An  that represents the return value generated by the asynchronous
	 * operation.
	 */
	function EndInvoke(result:cs.system.IAsyncResult):Dynamic;
	/**
	 * Synchronously executes the delegate on the thread that created this object and
	 * marshals the call to the creating thread.
	 * @param method A  that contains a method to call, in the context of the thread
	 * for the control.
	 * @param args An array of type  that represents the arguments to pass to the given
	 * method. This can be  if no arguments are needed.
	 * @return An  that represents the return value from the delegate being invoked, or
	 * if the delegate has no return value.
	 */
	function Invoke(method:cs.system.Delegate, args:cs.NativeArray<Dynamic>):Dynamic;
}
