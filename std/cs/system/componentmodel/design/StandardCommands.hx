package cs.system.componentmodel.design;

/** Defines identifiers for the standard set of commands that are available to most applications. */
@:native("System.ComponentModel.Design.StandardCommands")
extern class StandardCommands {
	/** Gets the  for the AlignBottom command. This field is read-only. */
	static var AlignBottom(default, never):cs.system.componentmodel.design.CommandID;
	/** Gets the  for the AlignHorizontalCenters command. This field is read-only. */
	static var AlignHorizontalCenters(default, never):cs.system.componentmodel.design.CommandID;
	/** Gets the  for the AlignLeft command. This field is read-only. */
	static var AlignLeft(default, never):cs.system.componentmodel.design.CommandID;
	/** Gets the  for the AlignRight command. This field is read-only. */
	static var AlignRight(default, never):cs.system.componentmodel.design.CommandID;
	/** Gets the  for the AlignToGrid command. This field is read-only. */
	static var AlignToGrid(default, never):cs.system.componentmodel.design.CommandID;
	/** Gets the  for the AlignTop command. This field is read-only. */
	static var AlignTop(default, never):cs.system.componentmodel.design.CommandID;
	/** Gets the  for the AlignVerticalCenters command. This field is read-only. */
	static var AlignVerticalCenters(default, never):cs.system.componentmodel.design.CommandID;
	/** Gets the  for the ArrangeBottom command. This field is read-only. */
	static var ArrangeBottom(default, never):cs.system.componentmodel.design.CommandID;
	/** Gets the  for the ArrangeIcons command. This field is read-only. */
	static var ArrangeIcons(default, never):cs.system.componentmodel.design.CommandID;
	/** Gets the  for the ArrangeRight command. This field is read-only. */
	static var ArrangeRight(default, never):cs.system.componentmodel.design.CommandID;
	/** Gets the  for the BringForward command. This field is read-only. */
	static var BringForward(default, never):cs.system.componentmodel.design.CommandID;
	/** Gets the  for the BringToFront command. This field is read-only. */
	static var BringToFront(default, never):cs.system.componentmodel.design.CommandID;
	/** Gets the  for the CenterHorizontally command. This field is read-only. */
	static var CenterHorizontally(default, never):cs.system.componentmodel.design.CommandID;
	/** Gets the  for the CenterVertically command. This field is read-only. */
	static var CenterVertically(default, never):cs.system.componentmodel.design.CommandID;
	/** Gets the  for the Copy command. This field is read-only. */
	static var Copy(default, never):cs.system.componentmodel.design.CommandID;
	/** Gets the  for the Cut command. This field is read-only. */
	static var Cut(default, never):cs.system.componentmodel.design.CommandID;
	/** Gets the  for the Delete command. This field is read-only. */
	static var Delete(default, never):cs.system.componentmodel.design.CommandID;
	/** Gets the  for the Document Outline command. This field is read-only. */
	static var DocumentOutline(default, never):cs.system.componentmodel.design.CommandID;
	/** Gets the  for the F1Help command. This field is read-only. */
	static var F1Help(default, never):cs.system.componentmodel.design.CommandID;
	/** Gets the  for the Group command. This field is read-only. */
	static var Group(default, never):cs.system.componentmodel.design.CommandID;
	/** Gets the  for the HorizSpaceConcatenate command. This field is read-only. */
	static var HorizSpaceConcatenate(default, never):cs.system.componentmodel.design.CommandID;
	/** Gets the  for the HorizSpaceDecrease command. This field is read-only. */
	static var HorizSpaceDecrease(default, never):cs.system.componentmodel.design.CommandID;
	/** Gets the  for the HorizSpaceIncrease command. This field is read-only. */
	static var HorizSpaceIncrease(default, never):cs.system.componentmodel.design.CommandID;
	/** Gets the  for the HorizSpaceMakeEqual command. This field is read-only. */
	static var HorizSpaceMakeEqual(default, never):cs.system.componentmodel.design.CommandID;
	/** Gets the  for the LineupIcons command. This field is read-only. */
	static var LineupIcons(default, never):cs.system.componentmodel.design.CommandID;
	/** Gets the  for the LockControls command. This field is read-only. */
	static var LockControls(default, never):cs.system.componentmodel.design.CommandID;
	/** Gets the  for the MultiLevelRedo command. This field is read-only. */
	static var MultiLevelRedo(default, never):cs.system.componentmodel.design.CommandID;
	/** Gets the  for the MultiLevelUndo command. This field is read-only. */
	static var MultiLevelUndo(default, never):cs.system.componentmodel.design.CommandID;
	/** Gets the  for the Paste command. This field is read-only. */
	static var Paste(default, never):cs.system.componentmodel.design.CommandID;
	/** Gets the  for the Properties command. This field is read-only. */
	static var Properties(default, never):cs.system.componentmodel.design.CommandID;
	/** Gets the  for the PropertiesWindow command. This field is read-only. */
	static var PropertiesWindow(default, never):cs.system.componentmodel.design.CommandID;
	/** Gets the  for the Redo command. This field is read-only. */
	static var Redo(default, never):cs.system.componentmodel.design.CommandID;
	/** Gets the  for the Replace command. This field is read-only. */
	static var Replace(default, never):cs.system.componentmodel.design.CommandID;
	/** Gets the  for the SelectAll command. This field is read-only. */
	static var SelectAll(default, never):cs.system.componentmodel.design.CommandID;
	/** Gets the  for the SendBackward command. This field is read-only. */
	static var SendBackward(default, never):cs.system.componentmodel.design.CommandID;
	/** Gets the  for the SendToBack command. This field is read-only. */
	static var SendToBack(default, never):cs.system.componentmodel.design.CommandID;
	/** Gets the  for the ShowGrid command. This field is read-only. */
	static var ShowGrid(default, never):cs.system.componentmodel.design.CommandID;
	/** Gets the  for the ShowLargeIcons command. This field is read-only. */
	static var ShowLargeIcons(default, never):cs.system.componentmodel.design.CommandID;
	/** Gets the  for the SizeToControl command. This field is read-only. */
	static var SizeToControl(default, never):cs.system.componentmodel.design.CommandID;
	/** Gets the  for the SizeToControlHeight command. This field is read-only. */
	static var SizeToControlHeight(default, never):cs.system.componentmodel.design.CommandID;
	/** Gets the  for the SizeToControlWidth command. This field is read-only. */
	static var SizeToControlWidth(default, never):cs.system.componentmodel.design.CommandID;
	/** Gets the  for the SizeToFit command. This field is read-only. */
	static var SizeToFit(default, never):cs.system.componentmodel.design.CommandID;
	/** Gets the  for the SizeToGrid command. This field is read-only. */
	static var SizeToGrid(default, never):cs.system.componentmodel.design.CommandID;
	/** Gets the  for the SnapToGrid command. This field is read-only. */
	static var SnapToGrid(default, never):cs.system.componentmodel.design.CommandID;
	/** Gets the  for the TabOrder command. This field is read-only. */
	static var TabOrder(default, never):cs.system.componentmodel.design.CommandID;
	/** Gets the  for the Undo command. This field is read-only. */
	static var Undo(default, never):cs.system.componentmodel.design.CommandID;
	/** Gets the  for the Ungroup command. This field is read-only. */
	static var Ungroup(default, never):cs.system.componentmodel.design.CommandID;
	/** Gets the first of a set of verbs. This field is read-only. */
	static var VerbFirst(default, never):cs.system.componentmodel.design.CommandID;
	/** Gets the last of a set of verbs. This field is read-only. */
	static var VerbLast(default, never):cs.system.componentmodel.design.CommandID;
	/** Gets the  for the VertSpaceConcatenate command. This field is read-only. */
	static var VertSpaceConcatenate(default, never):cs.system.componentmodel.design.CommandID;
	/** Gets the  for the VertSpaceDecrease command. This field is read-only. */
	static var VertSpaceDecrease(default, never):cs.system.componentmodel.design.CommandID;
	/** Gets the  for the VertSpaceIncrease command. This field is read-only. */
	static var VertSpaceIncrease(default, never):cs.system.componentmodel.design.CommandID;
	/** Gets the  for the VertSpaceMakeEqual command. This field is read-only. */
	static var VertSpaceMakeEqual(default, never):cs.system.componentmodel.design.CommandID;
	/** Gets the  for the ViewCode command. This field is read-only. */
	static var ViewCode(default, never):cs.system.componentmodel.design.CommandID;
	/** Gets the  for the ViewGrid command. This field is read-only. */
	static var ViewGrid(default, never):cs.system.componentmodel.design.CommandID;
	function new():Void;
}
