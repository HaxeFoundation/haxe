package unit.issues;

import String.fromCharCode as Math;
import Math;
import utest.Assert;

class Issue9197 extends Test {
	function testFielsVsTypeImport() {
		feq(1.0, Math.abs(-1));
		Assert.pass();
	}
}
