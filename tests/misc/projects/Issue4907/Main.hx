package;

class Main {
    static function main() {
        var v:EnumAbstract;
        switch (v) {
            case VALUE:
        }
    }
}

enum
abstract EnumAbstract(Int) {
    public static var VALUE = 0;
}